name: Frontend
on: [workflow_dispatch]  # вручную
jobs:
  print:
    runs-on:  ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hello GH Actions!
      - name: print a few lines
        run: | 
          echo First line!
          echo second line!

